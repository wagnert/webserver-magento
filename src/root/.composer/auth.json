{
    "github-oauth": {
        "github.com": "{{COMPOSER_OAUTH_GITHUB}}"
    },
    "http-basic": {
        "repo.magento.com": {
            "username": "{{COMPOSER_REPO_USER}}",
            "password": "{{COMPOSER_REPO_PASSWORD}}"
        }
    }
}